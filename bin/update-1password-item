#!/usr/bin/env bash
if [ $# -ne 3 ]; then
  echo "Error: update-1password-item requires 3 arguments: item_name, field, password" >&2
  exit 1
fi

item_name="$1"
field="$2"
password="$3"

op item edit --account "$ONE_PASSWORD_ACCOUNT" "$item_name" "${field}[password]=${password}"
