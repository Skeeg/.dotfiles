# Organizational Context Files
.npmrc
.yarnrc*
.profile.d/_personalprefs.plugin.zsh
.profile.d/_personalprefs.plugin.sh

# Machine-local shell overrides — not tracked, not symlinked
.zshrc.local

# Dotfiles sync throttle timestamp — local state, not shared
.dotfiles_pull_ts

# Context Portal MCP
context_portal*

# VSCode Settings
.vscode/settings.json
